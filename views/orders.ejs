<%- include layouts/header.ejs %>

    <!-- main content
    <main class="">
        <h1 class="tex-center"> Orders </h1>
        <section class="container mb-1">
            <table id="orders">
                <thead>
                    <tr>
                      <th>Order#</th>
                      <th>Created On</th>
                      <th>Status</th>
                      <th></th>
                    </tr>
                  </thead>
                 <tbody>
                    <% totalOrders = 0 %>
                    <% @orders.each do |order| %>
                    
                  <% if order.user.email == @user.email %>
                  
                    <% totalOrders += 1 %>
                      <tr>
                        <td><%= orders.id %></td>
                        <td><%= orders.created_at %></td>
                        <td><%= orders.status %></td>
                        <td><button target="" class="btn-checkout" role="button" aria-pressed="true"><%= link_to 'Show', order %></button></td>
                      </tr>
                    <% end %>
                    <% end %>
                    
                    <% if totalOrders == 0 %>
                    <tr>
                      <td colspan="3">No orders to display</td>
                    </tr>
                    <% end %>
                  </tbody>
            </table>
        </section>
    </main> -->
   <%- include layouts/footer.ejs %>
